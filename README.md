# 越野跑比赛时间概率预测系统 V0.2

本地读取多个历史 FIT 文件，建立个人平路、爬坡、下坡和长距离疲劳画像，再识别 GPX 中的自然爬坡、下降和平路并预测完成时间。

## 安装

需要 Python 3.11+：

```powershell
cd trail_predictor
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

开发和测试环境可改用 `pip install -r requirements-dev.txt`。

## 命令行使用

将历史文件放入 `data/activities/`，比赛路线放入 `data/races/race.gpx`，然后运行：

```powershell
python main.py --aid-minutes 20
```

也可以指定路径和分段长度：

```powershell
python main.py --activities D:\runs --race D:\races\race.gpx --output output --segment-distance 100
```

输出包括：

- `runner_profile.json`：个人能力画像
- `race_segments.json`：路线分段
- `prediction.json`：逐段预测明细
- `report.md`：预测报告
- `elevation_profile.png`：相对海拔走势

## Streamlit 界面

```powershell
streamlit run app.py
```

Windows 下也可以直接双击 `start_web.bat`，它会使用项目虚拟环境启动本地网页并自动打开浏览器。网页支持：

- 多选上传历史 Activity FIT
- 上传一个比赛 Race GPX
- 实时查看解析与计算进度
- 在网页中查看预测概览、个人能力、自然坡分段和完整报告
- 下载 Markdown 报告和预测 JSON

运行测试：

```powershell
python -m pytest -q
```

## 模型口径

- 个人地形能力：历史 FIT 与比赛 GPX 使用相同的自然坡算法，按100米窗口识别连续爬坡、下降和平路，再分别计算 VAM、下坡速度和平路配速。
- 坡度档位：上坡分为微坡1%–5%、缓坡5%–10%、中坡10%–15%、陡坡15%以上；下坡使用对称的四档。报告同时展示每档的样本数、距离、垂直高度和能力值。
- 平路：不使用心率，也不再要求连续1公里。越野自然平路样本占70%；路跑自然平路配速先乘1.10折算装备与路面影响，再占30%。仅有一种数据时自动使用对应来源。
- 上坡：按 5%–10%、10%–15%、15% 以上计算 VAM。
- 下坡：按坡度区间计算水平速度，同时记录垂直下降速度。
- 比赛路线：默认按100米采样坡度，平滑海拔后识别连续自然爬坡/下降，并合并坡中不超过200米的短暂起伏；最终预测段长度由地形决定，不再固定500米硬切。
- 疲劳：比较 0–3h、3–5h、5h 以后速度，数值代表能力保留比例；预测耗时按 `基础耗时 / 疲劳比例` 修正。
- 缺少某类有效样本时使用保守默认值，画像中的活动摘要可帮助判断样本是否足够。

V0.1 未纳入天气、路面技术难度、海拔适应、补给点分布与停留行为。首次验收应使用“历史活动训练、已完赛 FIT 建模，另一场已知结果的 GPX 做回测”，并比较预测总时间与真实移动时间。

## V0.2 Phase 1 模型稳定性

当前版本已加入第一阶段的模型稳定性能力：

- 经验参数集中在 `config/defaults.yaml`
- FIT 与 GPX 数据质量诊断
- 平路、上坡、下坡能力可信度与样本覆盖信息
- 上坡 VAM 和下坡速度连续插值，避免坡度档位跳变
- 平路、上坡、下坡分别计算的地形归一化疲劳曲线
- 疲劳系数随时间连续插值
- 活动新旧程度和活动类型权重

输出 JSON 仍保留 V0.1 字段，现有 CLI、Streamlit 页面和报告可以继续使用。状态、比赛条件和概率预测属于 V0.2 Phase 2，本阶段尚未加入。

### Phase 1 更新记录

本次更新完成 V0.2 Phase 1，重点是提高基础能力模型和疲劳模型的稳定性与可解释性：

- 新增类型化能力数据结构，为平路、上坡、下坡和疲劳能力记录数值、单位、来源、样本覆盖及可信度。
- 默认能力、活动权重、可信度规则和疲劳曲线等经验参数统一迁移到 `config/defaults.yaml`。
- 平路能力加入活动新旧程度和活动类型权重，不再只按未加权的单一分位数计算。
- 上坡 VAM 和下坡水平速度改用连续坡度曲线插值，预测不会在坡度档位边界发生突变。
- 疲劳模型改为地形归一化计算，分别生成平路、上坡和下坡疲劳曲线，避免把路线地形变化误判为疲劳。
- 疲劳曲线按时间连续插值，消除 3 小时、5 小时等时间边界的突变。
- 疲劳曲线的 `0h = 100%` 是固定基准点，不属于统计估计，其可信度显示为“—（固定基准）”。
- 自然坡分段中的“疲劳因子”表示到达该段时对应地形能力的预计保留比例，分段耗时按 `基础耗时 / 疲劳因子` 修正。
- 新增 FIT 和 GPX 数据质量诊断；GPX 同时比较 50 米、100 米和 200 米重采样下的累计爬升稳定性。
- Streamlit 页面和 Markdown 报告新增能力可信度、数据质量及三类连续疲劳曲线展示。
- 保留 V0.1 输出字段和运行入口，现有命令行及网页使用方式不变。

测试覆盖由 8 项增加到 13 项，包括配置加载、类型化能力值、上下坡连续插值、疲劳连续插值、可信度、FIT/GPX 数据质量和新版能力画像结构。当前测试结果为 `13 passed`。

## V0.2 Phase 2 概率预测

Phase 2 在稳定能力模型之上增加了比赛时长适配、当天状态、比赛条件和概率区间：

- 按活动持续时间建立短时（0～2h）、中时（2～5h）、长时（5～10h）和超长（10h 以上）能力层。
- 目标比赛不会直接套用短距离高强度能力；系统会迭代估算完赛时长，并在相邻持续能力层之间连续插值。
- 持续能力和途中疲劳分开计算，避免将“长距离起跑强度折减”和“比赛后程疲劳”混为一项或重复折减。
- 缺少目标时长或对应地形样本时使用配置中的保守折减，同时降低可信度并给出风险提示。
- 支持手动设置当前状态、温度、湿度、技术难度、泥泞等级、夜间比例、高海拔系数、携带重量和补给停留。
- 技术难度、泥泞、夜间和负重按平路、上坡、下坡分别修正，不使用统一的不可解释总系数。
- 默认执行 3000 次 Monte Carlo 模拟，可配置为 1000～10000 次；能力可信度和 GPX 质量决定预测分布宽度。
- 输出最快合理时间 P10、中位预测 P50、保守预测 P90 和综合预测可信度。
- 明确区分标准能力移动时间、条件修正后移动时间、补给停留时间和最终完赛时间。
- 输出基础地形、目标时长适配、疲劳、状态、温湿度、技术、泥泞、夜间、海拔、负重及补给的时间影响。
- Streamlit 页面增加比赛条件输入、预测分布图、时间损耗拆解和分段条件系数。

### 时间损耗拆解说明

“基础地形耗时”是后续修正的基准时间，并不是额外损耗。系统先将 GPX 划分为自然平路、上坡和下坡，然后按个人综合地形能力逐段计算：

```text
平路基础耗时 = 距离 × 平路有氧配速

上坡基础耗时 = max(
    爬升高度 ÷ 连续坡度曲线插值得到的 VAM,
    距离 × 平路有氧配速
)

下坡基础耗时 = 距离 ÷ 连续坡度曲线插值得到的下坡速度
```

所有分段相加得到“基础地形基准时间”。此时尚未考虑目标比赛时长、途中疲劳、当天状态、天气、技术难度和补给停留。

完整时间关系为：

```text
基础地形基准时间
+ 目标时长适配
+ 疲劳影响
+ 当前状态影响
+ 技术难度影响
+ 泥泞影响
+ 夜间影响
+ 高海拔影响
+ 装备负重影响
+ 温湿度影响
+ 补给停留
= 最终完赛时间
```

“目标时长适配”用于将综合能力调整为适合本场预计持续时间的可持续能力。它可以为正数，也可以为负数：

- 正数：长距离或超长距离需要更保守的起始强度，因此增加预计时间。
- 负数：短距离比赛可以使用高于综合基准的可持续强度，因此减少预计时间。
- `0分钟`：目标时长与综合能力基准基本一致。

例如：

```text
基础地形基准时间：1小时37分钟
目标时长适配：-7分钟
疲劳增加：12分钟
技术难度增加：5分钟
最终移动时间：1小时47分钟
```

负数时间会按带符号时长显示，例如 `-7分钟` 或 `-1小时07分钟`，不会再被错误拆分为 `-1小时53分钟`。

命令行可选参数示例：

```powershell
python main.py `
  --current-form slight_fatigue `
  --temperature 28 `
  --humidity 80 `
  --technical-level 3 `
  --mud-level 2 `
  --night-ratio 0.4 `
  --altitude-factor 1.05 `
  --carried-weight 1.5 `
  --aid-minutes 20
```

Phase 2 完成后测试覆盖为 16 项，新增持续能力层、比赛条件单调性、概率区间顺序和 Monte Carlo 固定随机种子复现测试。历史比赛解释与多场校准仍属于 Phase 3，本阶段不会使用单场比赛直接修改个人能力。
