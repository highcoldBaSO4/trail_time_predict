越野跑比赛时间预测系统 - Windows 便携版

使用方法：
1. 保持整个 TrailTimePredictor 文件夹完整，不要只复制 EXE。
2. 双击 TrailTimePredictor.exe。
3. 等待控制窗口显示“应用正在运行”，浏览器会自动打开。
4. 如果浏览器被关闭，可点击控制窗口中的“重新打开页面”。
5. 使用完成后点击“退出应用”或关闭控制窗口。

历史天气缓存和运行日志保存在：
%LOCALAPPDATA%\TrailTimePredictor

本程序只监听当前电脑的 127.0.0.1 地址，不会作为局域网服务器开放。
